# How many top-ranked companies (by Investment Meter score) to surface per
# sector each week. Selection uses the cheap quant+checklist score (no AI
# spend) computed for the whole screening universe in config/watchlist.yaml;
# only the selected top_n_per_sector companies per sector then get the AI
# narrative/qualitative layer - see pipeline/main.py's two-phase run_full()
# and pipeline/scoring/screening.py::select_top_picks.
top_n_per_sector: 5
